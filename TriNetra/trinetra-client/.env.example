# TriNetra client environment configuration
# Copy to `.env` and fill in as needed.

# Optional: enables the Google Static Maps preview in the case-detail drawer.
# When unset, the map area shows a placeholder and the GPS coordinates
# remain visible (no keyless requests are sent to Google).
VITE_GOOGLE_MAPS_API_KEY=
